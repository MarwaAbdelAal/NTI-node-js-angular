{{>header}}
{{>navbar}}

{{#unless user}}
    <div class="alert alert-danger">User not found</div>
{{else}}

<table class="table table-bordered">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Email</th>
            <th>Age</th>
            <th>Gender</th>
            <th>Addresses</th>
            {{!-- <th>Actions</th> --}}
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{user._id}}</td>
            <td>{{user.name}}</td>
            <td>{{user.email}}</td>
            <td>{{user.age}}</td>
            <td>{{user.gender}}</td>
            <td>{{user.Addresses}}</td>
            {{!-- <td> --}}
                {{!-- <a href="/single/1" class="btn btn-success mx-2">Show</a> --}}
                {{!-- <a href="/edit/{{user._id}}" class="btn btn-warning mx-2">Edit</a> --}}
                {{!-- <a href="/del/{{user._id}}" class="btn btn-danger mx-2">Delete</a> --}}
            {{!-- </td> --}}
        </tr>
    </tbody>
</table>

{{/unless}}
{{>footer}}